# =============================================================================
# monAlpha — Environment Variables
# Copy this to .env.local and fill in your values
# =============================================================================

# Network: testnet | mainnet
NETWORK=mainnet

# Monad RPC (QuickNode, Alchemy, or public)
MONAD_RPC_URL=https://monad-mainnet.drpc.org
# MONAD_WSS_URL=wss://your-provider.com/ws

# nad.fun API
NAD_API_BASE=https://api.nadapp.net
NAD_API_KEY=

# DeepSeek V3 API (for agentic planner)
DEEPSEEK_API_KEY=

# SkillRegistry contract address (deployed on Monad)
NEXT_PUBLIC_SKILL_REGISTRY=

# Foundation wallet — receives analysis payments
NEXT_PUBLIC_FOUNDATION_WALLET=

# WalletConnect project ID (optional — enables WalletConnect, get one at https://cloud.walletconnect.com)
# NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# =============================================================================
# OpenClaw Integration (optional — enables agentic orchestration)
# =============================================================================

# Set to true to use OpenClaw Gateway for agent orchestration
USE_OPENCLAW=false

# OpenClaw Gateway URL (default: http://localhost:18789)
OPENCLAW_GATEWAY_URL=http://localhost:18789

# OpenClaw Gateway auth token (must match OPENCLAW_GATEWAY_TOKEN in openclaw config)
OPENCLAW_GATEWAY_TOKEN=

# =============================================================================
# Deployment (only needed for server deploy scripts)
# =============================================================================

# Server paths (used by deploy/deploy.sh)
# APP_DIR=/home/deploy/monalpha/skillmarket
# REPO_DIR=/home/deploy/monalpha

# Domain & SSL (used by deploy/setup-ssl.sh)
# DOMAIN=monalpha.xyz
# EMAIL=admin@monalpha.xyz

# Contract deployment (used by contracts/script/Deploy.s.sol)
# OWNER_ADDRESS=0xYourOwnerWallet
# FOUNDATION_ADDRESS=0xYourFoundationWallet
